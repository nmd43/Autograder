# Core app (Streamlit UI + Gemini)
streamlit>=1.28.0,<2.0.0
google-genai>=1.0.0

# Vector store + embeddings + cross-encoder reranking
chromadb>=0.5.0,<0.6.0
sentence-transformers>=2.7.0,<4.0.0

# File parsing
pypdf>=4.0.0
nbformat>=5.9.0

# NumPy: keep flexible; Chroma/torch stacks vary by platform
numpy>=1.26.0,<3.0.0

# Protobuf: Chroma/gRPC stacks often need a modern pin on Windows
protobuf>=4.25.0,<6.0.0
